(function (window, undefined) {
	'use strict';

	namespace('Lab');

	function WorkPlace(json) {

	}

	WorkPlace.prototype = {
		constructor : WorkPlace,
		init : function (json) {


		}


	};

	window.Lab = window.Lab || {};
	window.Lab.WorkPlace = WorkPlace;
}(window));